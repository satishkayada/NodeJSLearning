/**
 * let is block scope declaration keyword.
 * const is used to declare variable which is immutable or ready only. it can modify arrays and objects
 */
let numArray = [1, 2, 3, 4]
console.log(numArray.includes(2))