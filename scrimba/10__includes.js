/**
 * includes function used in array to check if paramter exists in array or not.
 * it will return true or false bases on condition.
 */
let numArray = [1, 2, 3, 4]
console.log(numArray.includes(2))