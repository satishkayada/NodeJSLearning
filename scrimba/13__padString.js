/**
 * Pad function to add something from begining of string or end of string
 */
const name = 'satish'

console.log('before pad:', name)
name.padStart(100)

console.log('after pad:', name)
console.log('after pad length of name', name.length)
